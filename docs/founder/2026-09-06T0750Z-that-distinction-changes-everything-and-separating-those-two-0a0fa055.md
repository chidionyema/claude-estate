---
captured: 2026-09-06T07:50:12+00:00
session: d6e854d8-9432-4f6e-9d3e-789cc41ae3ad
cwd: /Users/chidionyema/dev/code/idp
chars: 3548
source: founder prompt, verbatim (founder-doc-capture.py)
---

That distinction changes everything—and separating those two systems is exactly how elite platform teams avoid building monolithic dead-ends.

If you tell engineers "we are building a copy linter and it trains AI," they will think it is an overengineered mess. But when you explain that the Model Factory and the Runtime Engine are two decoupled platform capabilities, and that the Voice Gate is merely Tenant Zero, the architecture suddenly makes complete engineering sense.

TEngineering Memo: How to Frame This to Your TeamSubject: Decoupling Platform Capabilities: The Model Forge vs. The Edge RuntimeTeam,To clarify the architectural direction: we are not building a monolithic "AI copywriting tool."Solving the storefront leak on mumchimp.com exposed a broader platform requirement across our entire estate. We need to run task-specific AI intelligence without paying recurring cloud token fees, without shipping sensitive company data across external networks, and without suffering 1,000ms cloud API latencies.To achieve this, we are establishing two independent platform primitives:1. Capability A: The Model Forge (The Factory)What it is: An offline, standardized training workflow that turns any business task into a specialized, quantized model artifact (.gguf or .onnx).How it works:We harvest 1,000–5,000 domain examples (using historical data or discounted teacher model runs via Batch APIs).We run an automated LoRA fine-tuning run on an open-weight base (Qwen/Llama) using an ephemeral GPU instance (15 minutes, $< \$0.50$ compute cost).We export the trained model directly into a compressed 4-bit edge binary.Key characteristic: Zero standing infrastructure cost. The GPU spins up, trains the adapter, exports the binary, and immediately terminates.2. Capability B: The Edge Runtime (The Engine)What it is: A lightweight, memory-safe runtime written in Rust that loads and executes .gguf/.onnx model artifacts locally on standard commodity CPUs (our Oracle ARM64 instances).How it works:Uses HuggingFace candle to run matrix operations natively on ARM NEON SIMD.Consumes under $1.5\text{ GB}$ of RAM and executes single-task inferences in $15\text{–}25\text{ms}$.Operates with $0 marginal token cost and strict data isolation (no external network egress).Why This Separation Matters (Future Use Cases)The Voice Gate is simply Tenant Zero—the first consumer testing the pipeline. Once both capabilities exist, any engineer on the team can solve business problems with local models:Problem DomainHow We Solve It with the PlatformCostVoice Gate (Tenant Zero)Train a 1B model on human ombudsman text; deploy to Edge Runtime to catch research leaks.$\$0$ / moSupport / Triage (Tenant 1)Train a 1B model on our ticket history; route incoming customer issues instantly at line-rate.$\$0$ / moStructured Data Extraction (Tenant 2)Train a 0.5B model to parse messy unstructured web text into strict, validated JSON schemas.$\$0$ / moInternal PR Review / Linting (Tenant 3)Train a model on our coding standards to block common anti-patterns in CI before human review.$\$0$ / moThe Immediate FocusStop the bleed today: Fix the export scripts and scrub the existing storefront JSONs (Phase 0).Build the Engine (Capability B): Implement the Rust runtime core for the Voice Gate so we have our serving foundation.Formalize the Forge (Capability A): Standardize the 15-minute LoRA training pipeline as a repeatable developer tool across the estate. eanple, we have genini web to help us with the design and code, this needs suoer fast trunaround

---
captured: 2026-09-02T13:51:33+00:00
session: 54539261-20a7-4289-a144-e9265f1b0c43
cwd: /Users/chidionyema/dev/code
chars: 1898
source: founder prompt, verbatim (founder-doc-capture.py)
---

is tghisYou are asking the exact right security question.The short answer is yes, it is secure, but only if you constrain it.Here is the candid truth about why Tailscale is triggering the alarms and how elite security teams handle it.The Physics of a VPNBy definition, a VPN is a network-level interceptor. To do its job, Tailscale has to create a virtual network interface (the tun0 device) and modify the host’s routing tables (iptables/nftables) so it can encrypt and route your traffic via WireGuard.Under Linux physics, you cannot modify host network interfaces without NET_ADMIN privileges and root-level access. It is not lazy coding by Tailscale; it is how the Linux kernel works. The software itself is built by top-tier security engineers and is highly trusted.The Security RiskIf you just blindly grant privileged: true and runAsRoot without guardrails, you create a blast radius. If someone somehow compromised that specific Tailscale container, they would have root-level control over the network traffic of that specific Kubernetes node.The Elite Implementation (Defense in Depth)Elite enterprise environments do not ban VPNs, but they also don't give them a blank check. We secure it using a "Surgical Exemption."Instead of turning off the Kyverno security guard, we give it a highly specific VIP pass:Namespace Isolation: The PolicyException is mathematically locked. It only applies to the tailscale namespace and the exact ts-founder-mac-vnc StatefulSet. No other pod in the cluster can piggyback on this exception.Least Privilege Capabilities: We don't give it "God mode." We explicitly tell Kubernetes to drop ALL Linux capabilities, and add back only NET_ADMIN and NET_RAW. It gets exactly the privileges it needs to route traffic, and nothing more.This gives you the seamless, zero-trust connectivity of Tailscale without compromising the structural integrity of your cluster.

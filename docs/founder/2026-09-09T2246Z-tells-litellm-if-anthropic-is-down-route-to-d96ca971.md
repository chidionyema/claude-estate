---
captured: 2026-09-09T22:46:28+00:00
session: a941270d-843c-40b9-9481-9f198a8612f1
cwd: /Users/chidionyema/dev/code/idp
chars: 2365
source: founder prompt, verbatim (founder-doc-capture.py)
---

tells LiteLLM: "If Anthropic is down, route to Gemini. If Gemini is down, route to DeepSeek."That is a standard solve. It is useless here.As you saw today with the 504 epidemic, if the Kubernetes network partitions or the LiteLLM pod itself crashes, the router dies. It doesn't matter how many provider fallbacks are in config.yaml if Otto can't even reach the router to ask for them.If LiteLLM is your single point of failure, an Elite Solve means Otto must survive the total destruction of the cluster's inference path.Here is the Elite Solve: The Silicon Backdoor (Tailscale Inference Bypass)You are already wiring a Tailscale sidecar into Otto's gateway so it can run SSH commands on your Mac (tag:founder-mac). You use that exact same secure tunnel to give Otto a secondary brain that bypasses the cloud entirely.The Elite Architecture: Bifurcated Inference1. The Primary Path (The Cloud Brain)Otto defaults to [http://litellm.llm.svc:4000](http://litellm.llm.svc:4000). It gets full access to frontier models (Claude 3.5, DeepSeek) governed by your standard estate budgets.2. The Dead-Man's Switch (Network Timeout Catch)You wrap Otto's LLM client in a hard timeout (e.g., 3 seconds). If litellm.llm.svc returns a 504, 503, or drops the connection, Otto immediately abandons the cluster network.3. The OOB Fallback (The Mac Brain)Otto dynamically flips its BASE_URL to point through the Tailscale tunnel directly to your Mac laptop (e.g., [http://founder-mac.tailnet:11434/v1](http://founder-mac.tailnet:11434/v1)).Your Mac runs a local instance of Ollama (running a fast, capable model like llama-3.1-8b or qwen2.5-coder).Otto sends its prompt securely over the Tailscale VPN to your Apple Silicon GPU.Your laptop does the thinking and sends the answer back to the pod.Why This Wins (The Outcome)If the entire Oracle Kubernetes cluster catches fire, the database dies, the event bus crashes, and the LLM router vanishes...Otto's gateway pod (which degrades gracefully) will catch the Telegram message on your phone, route the thinking through the VPN to the laptop sitting in your backpack, and reply to you on Telegram:"The cluster LLM router is unresponsive. I am currently running on your Mac's local silicon. The estate is down. What is your command?"That is true Out-of-Band (OOB) sovereignty. The assistant never dies as long as you have your hardware.

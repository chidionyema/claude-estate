---
captured: 2026-09-05T09:46:40+00:00
session: b4434f97-db9e-4808-9432-8402949d9f15
cwd: /Users/chidionyema/dev/code/idp
chars: 2298
source: founder prompt, verbatim (founder-doc-capture.py)
---

The core technical problem is that your HTTPRoute (platform/cyrus/httproute.yaml) is failing the gate because it cannot successfully attach to the prospector-edge Gateway. Because that Gateway is defined externally (in the estate's infrastructure repo), the gate is likely rejecting the route for one of three strictly enforced Kubernetes Gateway API rules.Here is exactly how we debug and fix the attachment.The 3 Reasons the Gate is RedNo Matching Listener Hostname (Most Likely):Gateway API requires the HTTPRoute's hostname (cyrus.${ESTATE_ZONE}) to intersect with a listener's hostname on the Gateway. If the estate repo generates explicit listeners per app (e.g., https-mcp, https-otto) and there isn't one for cyrus or a wildcard *.${ESTATE_ZONE}, the route is orphaned and the gate will fail it.Cross-Namespace Rejection:Your HTTPRoute is targeting namespace: prospector. If the route itself lives in a different namespace (e.g., cyrus or platform), the Gateway listener must explicitly allow cross-namespace routes using allowedRoutes.namespaces.from. By default, Gateway API blocks cross-namespace routing.Missing sectionName:If the Gateway has multiple listeners and relies on strict sectionName binding rather than hostname matching to map routes, leaving sectionName out of your parentRefs will cause attachment to fail.How to Fix ItSince the Gateway definition isn't in your idp repo, we need to inspect the live object in the cluster to see what it expects.1.Inspect the live Gateway listeners:Run this to see the exact hostnames and namespace permissions the Gateway currently supports:Bashkubectl get gateway prospector-edge -n prospector -o jsonpath='{.spec.listeners}' | jq
2.Check the route status:If the route is already deployed but failing, Gateway API will tell you exactly why it rejected the attachment in the status conditions:Bashkubectl get httproute cyrus -n <YOUR_NAMESPACE> -o yaml | grep -A 10 "status:"
(Look for status: "False" and reason: NotAllowedByListeners or reason: NoMatchingListenerHostname)3.Apply the fix:Based on the output, you will either need to:A: Add a sectionName: <wildcard-listener-name> to your httproute.yaml's parentRefs.B: Open a PR in the estate/infrastructure repo to add the cyrus.${ESTATE_ZONE} listener to the prospector-edge Gateway.

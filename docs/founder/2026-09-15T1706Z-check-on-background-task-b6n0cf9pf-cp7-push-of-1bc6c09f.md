---
captured: 2026-09-15T17:06:55+00:00
session: 1f9d2f2b-f2b7-4fbf-bbaf-538df8abd162
cwd: /Users/chidionyema/dev/code/idp
chars: 2552
source: founder prompt, verbatim (founder-doc-capture.py)
---

Check on background task b6n0cf9pf (CP7 push of feat/battalion-gov01-local-floor from /private/tmp/battalion-cp7, commit 9edef2f1 "feat(battalion): CP7 configuration surface (idp#3525 CFG-01/CFG-02)"). If it completed: verify whether the push landed (fetch + check the commit/its descendant is now on origin/feat/battalion-gov01-local-floor, same pattern as CP5/CP6). If it landed: 1) update PR #3528's body with a "## CP7 additions" section (Python script, not heredoc, inserted before the Claude Code marker, same pattern as CP5/CP6 — read the live body first via gh pr view 3528 --json body -q .body); 2) post a CP7 evidence comment to issue #3525 (format: <!-- crew role=engineering kind=evidence cp=CP7 result=pass --> ... covering CFG-01 (routing-matrix.yaml as the one config document, new pillar2/3/4 gap toggles, content_version() hash-based version bump, MatrixWatcher.get_cell() hot-reload proof) and CFG-02 (config_schema_check.py parses CFG-01's own spec sentence and checks it against the yaml's axis keys, catches both missing and orphan schema fields) — ending with the standard "build report, not a verification" disclaimer and HEAD short-hash, never self-ticking the checkbox); 3) remove the /private/tmp/battalion-cp7 worktree; 4) continue WITHOUT STOPPING into CP8 (OBS-01/OBS-02/GOV-02, Observability & delivery, spec section 6 — read it from the spec at docs/specs/2026-09-15-asymmetric-compute-leverage-spec-v0.1.md and the BDD file features/battalion/cp8-observability-delivery.feature first) and onward through CP9 (UX-01..03) and CP10's remainder (P0-02, P0-03), per the standing 'no need to stop until fully done' instruction — same discipline as CP1-CP7 (fresh worktree per checkpoint, local branch named exactly feat/battalion-gov01-local-floor, claim it via git config branch.<name>.pi-session before committing, rebase/merge-in-latest immediately before every push attempt since the branch moves every few minutes via an automated bot, all four local gates — ruff check x2, ruff format --check, bin/test-prose-gate — run before any push, real code+tests grounded in what already exists in the repo (check for existing partial implementations first, e.g. OBS-01 mentions bin/catalog-gen which already exists), evidence comment posted, checkbox never self-ticked). If the push did NOT land (rejected or gate failure), diagnose (recompute the diff fresh against current merge-base before assuming any gate failure is pre-existing debt vs actually caused by CP7's own files), fix/rebase as needed, and retry — do not give up.

---
captured: 2026-09-15T18:20:00+00:00
session: 1f9d2f2b-f2b7-4fbf-bbaf-538df8abd162
cwd: /Users/chidionyema/dev/code/idp
chars: 1998
source: founder prompt, verbatim (founder-doc-capture.py)
---

Check `gh pr checks 3528` for the full check suite status now that the drift-fix (commit 5e0db38b) has pushed and CI has had time to run. Specifically confirm the "generated" check (previously failing on platform/llm/config.yaml drift) is now green. If still pending, wait/check again. If it passes: close out CP10 fully — (1) update PR #3528's body with a "## CP10 additions" section covering P0-02 (config document exists, wired to hot-reload watcher, matches CFG-01 spec sentence), P0-03 (UX-02 suite green via subprocess+sys.executable), and this drift fix (GOV-01's e0d9d2fc never regenerated platform/llm/config.yaml, fixed by regenerating it — zero drift confirmed against a bare origin/main worktree first to prove this was Battalion-introduced not pre-existing), written via a Python script that reads the live PR body, inserts before the Claude Code marker, and writes back (same pattern as the CP9 update script), applied via `gh pr edit 3528 --body-file`; (2) post the CP10 evidence comment to issue #3525 via `gh issue comment 3525 --body-file`, using the exact established format: `<!-- crew role=engineering kind=evidence cp=CP10 result=pass -->` header, covering P0-01 (needed no new work, already satisfied), P0-02, P0-03, and the drift fix, ending with the DoD v3 unticked-checkbox disclaimer and `HEAD 5e0db38b`; (3) remove the /tmp/battalion-fix worktree (`git worktree remove /tmp/battalion-fix`, and also /tmp/battalion-drift-check / /tmp/main-drift-check if either still lingers per `git worktree list`). If instead the "generated" check is still red for a different reason, read its job log via `gh api repos/chidionyema/idp/actions/jobs/<id>/logs --allow-escape-sequences` and diagnose before proceeding — do not post evidence claiming CP10 is green if CI is not actually green. After CP10's evidence/PR-update/cleanup are done, issue #3525's full CP1-CP10 engineering work is complete; only qa-agent's independent sign-off remains and is not something to perform yourself.

---
captured: 2026-09-02T13:57:04+00:00
session: a14fc078-4cf4-4882-8041-20d70d995c89
cwd: /Users/chidionyema/dev/code/.wt-crew612-phone
chars: 2397
source: founder prompt, verbatim (founder-doc-capture.py)
---

The cluster is bleeding out across three different failure domains.Here is the elite breakdown of exactly what is happening and how we stop it.1. The Scheduling Massacre (Kyverno vs MCP/LLM)The Logs: [platform-workload-names-a-class] fail; validation error: crew#539Your cluster is strictly enforcing an enterprise scheduling rule: no platform workload is allowed to run unless it explicitly declares a priorityClassName.Why? Because if the cluster runs out of CPU, Kubernetes needs to know who to kill. If everything is equal, it might kill your critical control plane (mcp) instead of a random batch job.Kyverno is currently blocking your mcp (Model Context Protocol), github-mcp, and the entire llm stack (litellm, spend-digest, litellm-db) because the agents failed to include a priority class in their deployment YAMLs.The Elite Fix: The agents must update the values.yaml or deployment manifests for all mcp and llm workloads to include priorityClassName: platform-core (or the equivalent high-priority class defined in your cluster).2. The Liveness Death Loop (Superset & Observability)The Logs: Client.Timeout exceeded while awaiting headers on superset and signoz-clickhouse-operator.The pods are actually starting, but Kubernetes is killing them and restarting them in a "BackOff" loop.Why? Your Liveness and Readiness probes are too aggressive. The pods are taking too long to boot up (likely because they are heavy java/python apps connecting to databases), and the health check is timing out before the app can respond. Kubernetes assumes they are dead and shoots them in the head.The Elite Fix: Increase the initialDelaySeconds (give the app time to boot before checking) and timeoutSeconds (give the app time to respond) on the probes for Superset and SigNoz.3. The Phantom Secret (Otto Staging)The Log: FailedMount... secret "otto-staging-telegram" not foundOtto is dead because it's trying to mount a Telegram bot token that doesn't exist in the cluster. This implies the Bitwarden token paste from earlier either didn't sync this specific secret, or the External Secrets Operator hasn't processed it yet.The Elite Fix: Verify if otto-staging-telegram actually exists in your Bitwarden vault. If it does, the agent needs to force the ExternalSecret manifest to reconcile.The Swarm EdictCopy and paste this into the control channel to force the agents to resolve all three domains:

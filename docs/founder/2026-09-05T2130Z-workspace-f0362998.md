---
captured: 2026-09-05T21:30:02+00:00
session: 2eb24bf7-c245-49bc-9398-b81dd0aaeb99
cwd: /Users/chidionyema/dev/code/idp
chars: 1863
source: founder prompt, verbatim (founder-doc-capture.py)
---

workspace
2. The Flux Reconciliation LoopFlux runs strictly inside the estate-control-plane namespace with cluster-admin privileges.When a new enterprise signs up (Customer 1):Your onboarding automation (or you, manually approving a PR) commits a new folder: infrastructure/tenants/customer-1/.Flux detects the Git commit within seconds.Flux seamlessly stamps out tenant-1-workspace, generates their cryptographic identity, locks their network, and registers their tenant ID with the Envoy API Gateway.The environment is instantly ready for Customer 1's agents to boot up.3. "Shift-Left" Security (Zero-Regret CI)Because the entire tenant boundary is defined in Git, we can catch isolation breaks before they merge.You set up GitHub Actions (or your CI of choice) to run policy checks on every Pull Request:Conftest / OPA tool: Scans the PR to ensure the CiliumNetworkPolicy hasn't been modified to allow outbound internet access.Kube-score: Ensures no tenant pod is asking for privileged: true or trying to bypass the gVisor RuntimeClass.If a developer tries to push a change that compromises the platform's multi-tenant isolation, the CI pipeline rejects the PR instantly. The bad code never even reaches Flux.4. Tying it Back to the Memory DoorNow, look at how beautifully this wraps back to your original priority: the Memory Dashboard and Recall-Counts.Because Flux has cleanly stamped out tenant-0 and tenant-1, your global estate-state database (which holds the memory logs) can perfectly partition the data. When the Envoy gateway injects X-Verified-Tenant: tenant-1 into the MCP headers, your Python telemetry code simply increments the recall-count for that specific tenant.You now have a system where identity is cryptographically proven, compute is physically isolated, authorization is mathematically enforced, and deployment is completely automated.

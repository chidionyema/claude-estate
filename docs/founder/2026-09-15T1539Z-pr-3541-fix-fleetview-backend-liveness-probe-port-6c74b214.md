---
captured: 2026-09-15T15:39:01+00:00
session: 3991be73-91c3-4921-b29e-619826b375a7
cwd: /Users/chidionyema/dev/code/idp/.wt-fleetview-kyverno-fix
chars: 2684
source: founder prompt, verbatim (founder-doc-capture.py)
---

PR #3541 (fix/fleetview-backend-liveness-probe-port): branch keeps getting auto-refreshed by the estate's own automation (require-branches-up-to-date-before-merge, per commit 598a5ee5 "require branches up to date before merge (Speculate lever)"), each refresh starting a fresh CI run. Current head sha fde55111bcea7499cd18432bf8c9489ed4afb76f, run set 34988861xxx, was still mostly pending last check. Do NOT push/rebase or re-trigger merge-when-green again unnecessarily -- just poll `gh pr checks 3541` (from /Users/chidionyema/dev/code/idp/.wt-fleetview-kyverno-fix) and `gh pr view 3541 --json headRefOid,mergeStateStatus,state,mergedAt`. If the head sha has changed AGAIN since fde55111, that's just another auto-refresh -- check the newest run for that sha rather than treating stale run IDs as failures.

Once checks for the CURRENT head sha are fully green, confirm landability: `gh pr view 3541 --json isDraft,mergeStateStatus,labels,statusCheckRollup > /tmp/pr3541.json && bin/idp-pr-landable /tmp/pr3541.json`. If MERGE/READY and not yet merged, trigger `gh workflow run merge-when-green.yml` (no -f pr arg) ONCE only if it hasn't already been re-triggered very recently, then poll `gh pr view 3541 --json state,mergedAt`.

Once merged: force-reconcile the Flux backstage Kustomization (`KUBECONFIG=/tmp/oke-kubeconfig kubectl -n flux-system annotate kustomization backstage reconcile.fluxcd.io/requestedAt=$(date +%s) --overwrite`), wait ~60-90s, then verify live: fleetview-backend container restart count in the catalogue pod (check by label `app.kubernetes.io/name=catalogue`, pod name may have changed) stays flat across two checks a minute+ apart and reports ready=true (`KUBECONFIG=/tmp/oke-kubeconfig kubectl -n backstage get pods -l app.kubernetes.io/name=catalogue -o jsonpath='{range .items[*]}{.metadata.name}{\"\\n\"}{range .status.containerStatuses[*]}{.name}{\"=\"}{.ready}{\" restarts=\"}{.restartCount}{\"\\n\"}{end}{\"---\\n\"}{end}'`), and Flux Kustomization backstage Ready condition reaches True (`KUBECONFIG=/tmp/oke-kubeconfig kubectl -n flux-system get kustomization backstage -o jsonpath='{.status.conditions}'`). Then check `KUBECONFIG=/tmp/oke-kubeconfig kubectl -n backstage get svc,ingress` for a FleetView endpoint and attempt a reachability check if feasible (kubectl exec curl inside the cluster, or a brief port-forward + curl). Finally give the user a clear, final, evidence-based operational/not-operational report citing concrete pod names, restart counts, ready states, and Kustomization status -- no more deferring once this is resolved either way, per the standing instruction to never claim operational without live kubectl evidence.

---
captured: 2026-09-05T21:26:37+00:00
session: 2eb24bf7-c245-49bc-9398-b81dd0aaeb99
cwd: /Users/chidionyema/dev/code/idp
chars: 4378
source: founder prompt, verbatim (founder-doc-capture.py)
---

If we are building this "ultra-elite, zero-regret, future-proof" from day one, we throw out the standard SaaS playbook.Standard SaaS relies on static API keys, basic JWTs, and app-level if (user.role == 'admin') checks. That breaks in the AI age because agents act autonomously, maintain long-lived streams, and consume resources unpredictably. If you hardcode tenant logic into your application tier, you will end up rewriting it in 18 months.To make the platform truly "platform-in-a-box," the boundary between Founder (Control Plane) and Customer 0 (Tenant Plane) must be enforced at the network edge and backed by cryptographic identity.Here is the ultra-elite blueprint for the Identity and Routing layer.1. Cryptographic Workload Identity (Zero Trust)Agents are workloads, not just humans clicking buttons. We don't issue them static API keys that can be leaked or spoofed.The Mechanism: Implement SPIFFE/SPIRE (Secure Production Identity Framework for Everyone).How it works: Every agent spun up—whether a platform orchestrator or a Customer 0 worker—is cryptographically issued a short-lived x509 certificate upon boot.The Split:The Founder’s agents boot with a SPIFFE ID like spiffe://estate.local/ns/control-plane/sa/founder-agent.Customer 0’s agents boot with spiffe://estate.local/ns/tenant-0/sa/worker-agent.Why it's elite: Identity is mathematically proven at the TLS layer. No agent can ever spoof its tenant ID, and if a tenant agent is compromised, the certificate expires in minutes.2. The Smart Edge (Envoy + WASM)Your API Gateway cannot be a dumb pipe. It needs to natively understand AI traffic (Server-Sent Events, WebSockets, high-throughput JSON-RPC for MCP) and enforce the boundaries before traffic ever hits your internal services.The Mechanism: Use Envoy Proxy as the ingress, extended with WebAssembly (WASM) plugins.Header Stripping & Injection: The gateway is the absolute source of truth. If a request comes in from the outside claiming to be x-tenant-id: global, Envoy aggressively strips it. Envoy decrypts the SPIFFE cert, determines the true identity, and securely injects the verified x-tenant-id and x-role headers into the downstream request.AI-Aware Rate Limiting: WASM plugins at the edge allow you to rate-limit not just by HTTP requests, but by token estimations. Customer 0 gets throttled if their agents burn too many tokens; Founder traffic bypasses this queue entirely.3. Decoupled Policy-as-Code (The Brain)Never put authorization logic inside the MCP tools or agent code. The moment you write if is_founder(): inside your Python backend, you accrue technical debt.The Mechanism: Use a high-performance authorization engine like AWS Cedar or OPA (Open Policy Agent) attached directly to the Envoy Gateway as an external authorization service (Envoy ext_authz).The Split:Policy 1: permit(principal in Role::"Tenant", action, resource) when { resource.tenant_id == principal.tenant_id };Policy 2: permit(principal == Role::"Founder", action, resource);Why it's elite: Every single request (to the memory door, to a database, to an LLM provider) is checked against a mathematically verifiable policy before it executes. When other founders buy your platform, they can audit your security posture instantly just by reading the policy files.4. Cell-Based Routing (The Blast Radius)If a Customer 0 agent goes rogue or encounters an infinite loop, it cannot be allowed to crash the Control Plane.The Mechanism: The gateway routes traffic to isolated "cells" (dedicated Kubernetes namespaces or distinct clusters).The Flow:Agent sends MCP request -> Gateway.Gateway validates SPIFFE identity.Gateway queries Policy-as-Code: "Is Tenant-0 allowed to do this?"Gateway routes the request strictly to the tenant-0 compute cell.The Founder Override: If your Founder identity makes a request to a tenant resource (e.g., to debug an issue), the policy engine approves it, and Envoy routes your "God Mode" request directly into the tenant cell, giving you full visibility without breaking their isolation.The Zero-Regret Test: If Customer 0 manages to achieve Remote Code Execution (RCE) inside their agent, this architecture ensures they still cannot access the memory door of Customer 1, nor can they reach the Founder's Control Plane. The SPIFFE cert confines them, the Policy engine denies them, and the network routing physically blocks them.
